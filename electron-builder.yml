appId: pe.clevererp.cleverprint
productName: Clever Print
copyright: © 2026 Clever ERP
asar: true
asarUnpack:
  - "**/node_modules/{usb,@serialport,node-thermal-printer}/**"
directories:
  output: dist
  buildResources: resources
files:
  - "out/**"
  - "package.json"
  - "!**/.cache/**"
extraResources:
  - from: "resources/tray-idle.png"
    to: "tray-idle.png"
  - from: "resources/tray-busy.png"
    to: "tray-busy.png"
  - from: "resources/tray-error.png"
    to: "tray-error.png"
win:
  target:
    - target: nsis
      arch: [x64]
  icon: resources/icon.ico
  # publisherName intentionally omitted until we have a code-signing cert.
  # If set, electron-updater verifies that downloaded installers are signed
  # by this publisher and refuses to install unsigned ones — which blocks
  # auto-update for our current unsigned releases. Once we buy EV / Azure
  # Trusted Signing, restore `publisherName: Clever ERP S.A.C.` AND wire the
  # signing env vars (CSC_LINK + CSC_KEY_PASSWORD) in CI.
nsis:
  oneClick: true
  perMachine: false
  allowToChangeInstallationDirectory: false
  installerIcon: resources/icon.ico
  uninstallerIcon: resources/icon.ico
  shortcutName: Clever Print
  runAfterFinish: true
  deleteAppDataOnUninstall: false
mac:
  target: dmg
  category: public.app-category.business
linux:
  target: AppImage
  category: Utility
publish:
  provider: github
  owner: clever-erp
  repo: clever-print
  releaseType: release
